DATABASE_URL="mysql://USER:PASSWORD@HOST:3306/enercore_crm"
APP_URL="http://localhost:3000"
# Production needs a configured MySQL database. Preview contains fictional data only.
APP_MODE="preview"
# Set only when running the explicit bootstrap command against your approved database.
BOOTSTRAP_EMAIL=""
BOOTSTRAP_PASSWORD=""
BOOTSTRAP_CONFIRM=""
